const config = {
    bot: {
        prefix: 'announcer:', // Put the prefix you want commands to start with here
        token: 'Put_your_discord_token_here', // Put your Discord Bot Token here
        gameName: 'Game Name' // Put the playing status you want here
    },

    configVersion: '2' //Don't change this!
}

module.exports = config;